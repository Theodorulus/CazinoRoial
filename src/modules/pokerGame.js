// const { parentPort } = require('worker_threads')

// parentPort.once('message', (gameData) => {
//     const io = gameData.io
//     var connectedUsers = gameData.connectedUsers
//     const roomId = gameData.roomId
//     const roomInfo = gameData.roomInfo

//     currentPlayerId = 0;

//     io.on('connection', socket => {
//         console.log("Din worker connection: ", socket.id)
        
//     })

// })

// function pokerGame(io) {

// }